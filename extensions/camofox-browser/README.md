# camofox-browser extension

This extension keeps dependency manifests in source control but excludes installed packages.

## Development

Install dependencies locally when needed:

```bash
npm install
```

`node_modules/` is intentionally ignored to keep repository snapshots and backups small.
